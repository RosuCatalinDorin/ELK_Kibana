  pipeline {
   agent any
      stages {
          stage("build") {
            steps {
              echo 'bulding the application'
                     sh '''
                      pwd
                      echo "hello world"
                  '''
            }
             stage("deploy") {
                        steps {
                          echo 'deploying the aplication'
                        } }
           stage("test") {
            steps {
              echo 'testing the application'
            }
          }

          }
          }
     }

