  pipeline {
   agent any
      stages {
          stage("build") {
            steps {
              echo 'bulding the application'
              sh(
                git clone https://github.com/RosuCatalinDorin/ELK_Kibana.git
              )
            }
          }
           stage("test") {
            steps {
              echo 'testing the application'
            }
          }
              stage("deploy") {
            steps {
              echo 'deploying the aplication'
            }
          }
     }
  }
