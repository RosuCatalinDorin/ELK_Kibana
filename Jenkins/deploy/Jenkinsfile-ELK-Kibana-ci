  pipeline {
   agent any
      stages {
          stage("build") {
          agent {
                  label "master"
            }
            steps {
              echo 'bulding the application'
                     sh '''
                      pwd
                      echo "hello world"
                      docker-compose -up -d
                  '''
            }
          }
           stage("test") {
            steps {
              echo 'testing the application'
            }
          }
              stage("deploy") {
            steps {
              echo 'deploying the aplication'
            }
          }
     }
  }
