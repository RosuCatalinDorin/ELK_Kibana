  pipeline {
   agent any
      stages {
          stage("build") {
            steps {
              echo 'bulding the application'
                     sh '''
                      pwd
                      ls
                      echo "hello world"
                      cd ..
                      echo "copping file"
                      cp -r  ELK_Kibana /var/www/
                      cd /var/www/
                      pwd
                      sudo docker-compose up -d
                  '''
            }
            }
            stage("deploy") {
              steps {
                echo 'deploying the aplication'
                       }
            }
               stage("test") {
                steps {
                  echo 'testing the application'
                 }
             }

          }
   }


